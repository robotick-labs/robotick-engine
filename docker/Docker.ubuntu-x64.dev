FROM ubuntu:24.04

# 🛠️ Install base dependencies
RUN apt-get update && apt-get install -y \
    git \
    curl \
    build-essential \
    cmake \
    libopencv-dev \
    python3 \
    python3-pip \
    python3-venv \
    && apt-get clean

# 🧱 Root-level setup (install packages and MQTT libs)
COPY ./1_setup_my_env_as_root.ubuntu.sh /1_setup_my_env_as_root.ubuntu.sh
RUN chmod +x /1_setup_my_env_as_root.ubuntu.sh && /1_setup_my_env_as_root.ubuntu.sh

# 🧑 User-level setup script
COPY ./2_setup_my_env_as_user.ubuntu.sh /2_setup_my_env_as_user.ubuntu.sh

# Set working directory where setup.py lives
WORKDIR /workspace

# Copy all source files into container
COPY . .

# 🚀 Run user-level script (system-wide editable Python install)
RUN chmod +x ./2_setup_my_env_as_user.ubuntu.sh && ./2_setup_my_env_as_user.ubuntu.sh

# 🧼 Optional: default interactive shell
CMD ["bash"]
